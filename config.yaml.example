server:
  host: ""           # IP to bind to (empty = all interfaces, "10.0.0.10" = specific IP)
  port: 3333
  mode: development  # development or production

database:
  path: ./teleport.db

logging:
  level: info  # debug, info, warn, error
  format: json  # json or text
